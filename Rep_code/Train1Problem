🔍 关键问题分析
1. 数据问题
text
标签范围: nan ~ nan
标签均值: nan ± nan
标签列 (Fx_norm, Fy_norm, Fz_norm) 有 NaN 值

数据中有360个NaN，占总数的1.8%
✅ 数据修复成果
NaN值已处理：2160个NaN值通过前向填充处理

数据已标准化：

CapSense：均值567.36 → 标准化后 -1.34 ~ 2.45

IMU：均值4567.03 → 标准化后 -0.35 ~ 4.52

标签范围正常：-0.75 ~ -0.75（可能第一个样本比较特殊）



2. 维度不匹配警告
text
DenseBlock输出维度不一致（114 vs 115）
IMU DenseBlock输出115 ≠ CapSense输出114

3. 训练问题
text
损失值为nan
训练过程中出现NaN，导致损失无法计算


训练过程：
第10轮 → checkpoint_epoch_10.pth（存档1）
第20轮 → checkpoint_epoch_20.pth（存档2）
第30轮 → checkpoint_epoch_30.pth（存档3）
第40轮 → checkpoint_epoch_40.pth（存档4）
第50轮 → 
    ├→ checkpoint_epoch_50.pth（最终存档）
    ├→ subject_1_jogging_20251201_201658.pth（毕业模型）
    └→ results_subject_1_jogging_20251201_201658.pkl（成绩单）